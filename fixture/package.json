{
  "name": "forum-dev",
  "private": true,
  "workspaces": [
    "ui",
    "tests"
  ],
  "scripts": {
    "start": "AGENTS=${AGENTS:-2} BOOTSTRAP_PORT=$(get-port) npm run network",
    "network": "hc sandbox clean && npm run build:happ && UI_PORT=8888 concurrently \"npm run start --workspace ui\" \"npm run launch:happ --force-admin-ports $ADMIN_PORT,$ADMIN_PORT_2\"",
    "test": "npm run build:zomes && hc app pack workdir --recursive && npm run test --workspace tests",
    "launch:happ": "hc-spin -n $AGENTS --ui-port $UI_PORT workdir/forum.happ",
    "package": "npm run build:happ && npm run package --workspace ui && hc web-app pack workdir --recursive",
    "build:happ": "npm run build:zomes && hc app pack workdir --recursive",
    "build:zomes": "RUSTFLAGS='--cfg getrandom_backend=\"custom\"' cargo build --release --target wasm32-unknown-unknown"
  },
  "devDependencies": {
    "@holochain/hc-spin": "^0.600.0",
    "concurrently": "^6.5.1"
  },
  "engines": {
    "npm": ">=7.0.0"
  }
}